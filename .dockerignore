# Git
.git
.gitignore
.github

# CI / local tooling (not used inside the runtime image)
Dockerfile
.dockerignore
Makefile
.trivyignore
requirements-dev.txt
pyproject.toml
uv.lock
.python-version
.nvmrc
.pre-commit-config.yaml
renovate.json

# Documentation (markdown + screenshot binaries — keep out of runtime image)
README.md
*.md
docs/

# Dev-only / probe scripts (also gitignored; keep out of local docker builds)
scripts/
tools/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Application data (will be created in container)
data/
*.log
logs/

# Testing
tests/
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Local security / package audit caches (from Makefile or CLI)
.pip-audit-cache/

# Editor / agent (if present)
.cursor/

# Temporary files
temp/
tmp/
*.bak
*.backup
*.tmp

# Frontend build artifacts (source stays in context for frontend-builder;
# runtime-assembler copies only app paths + builder dist — not frontend/)
frontend/node_modules/
frontend/dist/

# Local development
.env.local
.env.development.local
.env.test.local
.env.production.local
